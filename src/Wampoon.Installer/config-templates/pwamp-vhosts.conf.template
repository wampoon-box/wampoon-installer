<VirtualHost *:80>
    ServerName localhost
    DocumentRoot "${DOCROOT}"

    # Redirect only if user hits exactly '/'
    #RedirectMatch "^/$" "/pwamp-dashboard"
    
	Alias /phpmyadmin "${PWAMP_APPS_DIR}\phpmyadmin"
	#Alias /pwamp-dashboard "${PWAMP_APPS_DIR}\pwamp-dashboard"

    <Directory "${PWAMP_APPS_DIR}\phpmyadmin">
		Options Indexes FollowSymLinks
		AllowOverride All
		Require all granted
	</Directory>

	#<Directory "${PWAMP_APPS_DIR}\pwamp-dashboard">
	#	Options Indexes FollowSymLinks
	#	AllowOverride All
	#	Require all granted
	#</Directory>
</VirtualHost>
